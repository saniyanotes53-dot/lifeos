{
  "name": "lifeos-functions",
  "description": "Firebase Cloud Functions for Life OS backend gateways (AI insights, Email digests, Timetable push reminders)",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "@google/genai": "^0.1.1"
  },
  "private": true
}
